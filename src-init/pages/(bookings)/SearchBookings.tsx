import { PageHeader } from "../../components";

const SearchBookings = () => {
    return (
        <>
            <PageHeader title="Search Bookings" />
            <div className="p-4">
                <p className="text-gray-500">
                    This is a demo placeholder for the dashboard.
                    You can either remove this file or replace it with a proper
                    component in <code>./src/pages/(bookings)/CompletedBookings.tsx</code>.
                </p>
            </div>
        </>
    );
};

export default SearchBookings;
